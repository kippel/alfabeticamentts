# Alfabeticament

```
$ npm i -g @nestjs/cli

$ nest new backend

backend $ npm i prisma -D
backend $ npx prisma init --datasource-provider sqlite

backend $ npx prisma migrate dev --name init
```

```
$ npx create-next-app@latest frontend

```
